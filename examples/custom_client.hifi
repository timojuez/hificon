#!/usr/bin/env -S python3 -m hificon.hifish

def on_shared_var_change(key, value):
    print("\nChanged %s to %s."%(key,value))

def volume_prompt():
    print("Current volume: %.1f" % $volume)
    while True:
        newvol = input("Enter new volume: ")
        $volume = Decimal(newvol)

if __name__ == "__main__":
    $bind(on_shared_var_change=on_shared_var_change)
    volume_prompt()

